// import { store as currentStore } from "~/composables/store";

export default function () {
  // const store = currentStore as any;
  // const { id: frameId } = frameStore.addFrame("Untitled");
  // for (const block of store.value.blocks) {
  //   blockStore.addBlock({
  //     ...block,
  //     frameId,
  //   });
  // }
  // store.value.blocks = undefined;
}
